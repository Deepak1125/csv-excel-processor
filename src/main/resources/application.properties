# Runtime configuration is supplied through environment variables or a secret manager.
db.url=${CSV_PROCESSOR_DB_URL:jdbc:mysql://localhost:3306/hr}
db.user=${CSV_PROCESSOR_DB_USER:}
db.password=${CSV_PROCESSOR_DB_PASSWORD:}
export.path=./output
